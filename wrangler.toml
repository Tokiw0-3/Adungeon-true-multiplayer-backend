name = "aidungeon-multiplayer-enhanced"
main = "worker.js"
compatibility_date = "2024-01-01"

# You can customize the worker name and route as needed
# After deploying, update the WS_URL_BASE in the userscript with your worker URL

[env.production]
name = "aidungeon-multiplayer-enhanced"

# Uncomment and configure if you want to use a custom domain
# route = "wss://your-custom-domain.com/*"
